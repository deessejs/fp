---
name: package-identity
description: @deessejs/fp is a real FP utilities package, not a template
type: user
---

This is **NOT a template** — it's a functional programming utilities package called `@deessejs/fp` published on npm (v1.0.0-alpha.1).

The repo is the actual package, not a template repo. Don't confuse it with the template it was initialized from.